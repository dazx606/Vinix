import { Text, View } from "react-native"

export default function AddScreen ({ route }){

    return(
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>aqui añadimos</Text>
        </View>
        
    )
}